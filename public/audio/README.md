# Ambient audio

Place a looping background track here as `ambient.mp3`. It stays silent until the `M` key (or the speaker button) is pressed — browsers block autoplay with sound until someone interacts with the page.

Keep it gentle and instrumental; it plays at 35% volume under the slides.
